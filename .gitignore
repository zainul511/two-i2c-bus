# 1. Ignore everything in the repository by default
*

# 2. Keep the .gitignore file itself (optional but recommended)
!.gitignore

# 3. Un-ignore the specific file and its parent directory
# If main.c is in a folder named 'main' (Standard ESP-IDF structure):
!main/
!main/hello_world_main.c

# If main.c is in the root directory:
!hello_world_main.c